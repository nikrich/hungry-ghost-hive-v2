# Phase 1.2 Verification Runbook — Executed

**Date executed:** 2026-05-28
**Hive binary commit:** `a93304a` (post Tasks 1-7; T8 verification commit appended after)
**Tested against:** claude `claude-opus-4-7`, mempalace gateway via `/Users/jannik/.agentflow/.venv/bin/python3` (editable install from `/Users/jannik/development/sanlam-digisure/sft-capstone-memory/gateway/src/mempalace_gateway/`)
**Test repo:** local bare remote `/tmp/hive-verify-bare` + working clone `/tmp/hive-verify-target`
**Predecessor:** [Phase 1.1 runbook](2026-05-28-phase-1.1-verification-runbook.md)

## Result summary

**Overall: PARTIAL PASS — `hive init` + workspace-local memory bootstrap fully work. Two new issues block the manager loop from completing a tick.**

**What worked (Phase 1.2 named goals):**
1. ✅ `hive init` auto-detected python3 at `/Users/jannik/.agentflow/.venv/bin/python3`, verified `mempalace_gateway` importable, skipped install
2. ✅ `.hive/memory/` skeleton created with all 5 rooms (`requirements`, `stories`, `agents`, `escalations`, `findings`), plus `.mempalace/config.yaml` with `allowlist: [hive]`, plus `index/`
3. ✅ `.hive/.gitignore` contains `memory/`
4. ✅ `.claude/mcp.json` written with workspace-local config: absolute python path, `mempalace_gateway.server` args, and `MEMPALACE_ROOT=/tmp/hive-verify-ws/.hive/memory`
5. ✅ Non-bare warning did NOT fire (we used the bare `/tmp/hive-verify-bare` as the team URL)
6. ✅ `hive add-req` wrote one inbox file
7. ✅ Watchdog spawned the manager subprocess with the correct disambiguated prompt

**What didn't work — two new blockers for Phase 1.3:**

| # | Issue | Severity |
|---|---|---|
| 1 | Manager process hung for the full 5-minute timeout, never wrote to `manager.log`. Watchdog killed it after `5m0.02117675s`. Session log (`a6cd9919-...jsonl`) shows the manager DID invoke `hive-v2-manager` 3 times — so the workspace-local skill IS being discovered — but most of the session time was spent on Bash commands exploring the workspace (`ls ~/.claude/skills/`, `find ~/.claude -maxdepth 5 ...`) and waiting on user-side plugin hooks (`pretooluse.py`, `posttooluse.py`, `on-post-tool-use.sh`). The manager never reached the inbox-drain step before the watchdog SIGKILL'd it. **Findings:** (a) the user's globally-configured plugin hooks add per-tool overhead that compounds across many Bash calls; (b) something in the manager skill triggered exploratory behavior instead of straight execution; (c) `manager.log` is empty because claude's stdout was buffered when SIGKILL hit — we lose all manager output on timeout. | **HIGH** |
| 2 | `hive status` errored with `MEMPALACE_ROOT environment variable not set`. The Go code in `cmd/hive/status_cmd.go` still calls `paths.MempalaceRoot()` (env var reader from Phase 0/1.1) — it doesn't yet know about workspace-local memory at `.hive/memory/`. The spec assumed the CLI would read from disk after Phase 1.2 made drawers land on disk, but the path-resolution code wasn't updated to point at the workspace-local memory dir. | **HIGH** (small fix — point status at `<workspace>/.hive/memory/`) |

**Inbox state after the run:** the original `req-1779975795-315d57c7.txt` file is still in `.hive/inbox/` (manager never drained it).

**On-disk drawers state:** the workspace `wings/hive/rooms/` directories are all empty. No drawer file got written (manager never got that far).

## Phase 1.2 deltas vs Phase 1.1

| Check | Phase 1.1 result | Phase 1.2 result |
|---|---|---|
| `hive init` installs mempalace if missing | n/a | ✅ no-op (already importable from `~/.agentflow/.venv`); fast |
| `hive init` writes workspace-local mcp.json | auto-detect from `~/.claude.json` | ✅ workspace-local config emitted; correct paths |
| `.hive/memory/` skeleton created | n/a | ✅ full tree |
| Wing is `hive` (not `hive-verify`) | `hive-verify` | ✅ fixed `hive` |
| Memory lives in the workspace (not `~/sft-capstone-memory/`) | ❌ shared global palace | ✅ `/tmp/hive-verify-ws/.hive/memory/` |
| On-disk drawers visible after first tick | ❌ no (chromadb only) | ❌ no (manager never drained inbox — timed out) |
| `hive status` shows non-zero counts | ❌ no | ❌ no — bug: still uses `MEMPALACE_ROOT` env var |
| Worker survived spawn (no immediate death) | ❌ no | ⚠️ untested (no worker spawned this run) |
| Story flipped to `review` or higher | ❌ no | ⚠️ untested |
| (If forced) abandoned story re-pended | n/a | ⚠️ untested |

## What the manager session actually did during its 5-minute hang

From `~/.claude/projects/-private-tmp-hive-verify-ws/a6cd9919-1276-462b-875d-da60e7fbf3a2.jsonl`:

- Invoked `hive-v2-manager` skill three times — the workspace-local skill discovery worked
- Used `Bash` for ~15+ exploratory commands (`ls`, `find ~/.claude`, etc.)
- Each `Bash` call was wrapped with the user's `pretooluse.py` + `posttooluse.py` hooks (per-tool latency)
- Ended on a `sleep 5; cat .../tasks/bw5x8040k.output` Bash command — looks like the manager was using its own background-task pattern and waiting on the result when SIGKILL hit
- Never wrote a single line to `manager.log` (stdout buffered + flushed-on-exit; SIGKILL preempted)

## Pre-flight (executed)

- [x] `which claude` → `/opt/homebrew/bin/claude`
- [x] `python3 -c "import mempalace_gateway"` exits 0 (verified post-`hive init`)
- [x] Test repo: `/tmp/hive-verify-target` → bare remote at `/tmp/hive-verify-bare`

## Issues filed for Phase 1.3

| Priority | Issue | Recommended fix |
|---|---|---|
| **HIGH** | Manager hangs / exploratory behavior leading to 5-min timeout | (a) tighten manager skill imperative wording further OR (b) use `--system-prompt-file` to inline the skill content so claude doesn't need to "discover and invoke" it OR (c) bump the watchdog timeout to ~10-15 min to absorb plugin-hook overhead. Probably (a) + (c) together. |
| **HIGH** | `manager.log` empty after timeout | Use unbuffered output (`stdbuf -o0`) or have the watchdog redirect via `tee -a` so partial output survives SIGKILL. Without this, every timeout is opaque. |
| **HIGH** | `hive status` doesn't know about workspace-local memory | Trivial Go fix: `cmd/hive/status_cmd.go` should resolve the wing root via `<workspace>/.hive/memory/wings/hive` instead of `paths.MempalaceRoot()`. Update `paths.go` or status_cmd.go directly. |
| MEDIUM | User-side plugin hooks add per-Bash-tool latency that compounds in long manager ticks | Out of hive's control; document so users can run hive with hooks disabled (`--bare` flag on the manager spawn?) |
| LOW | The auto-detected python is `~/.agentflow/.venv/bin/python3`, not anything hive controls. If the user removes that venv, hive breaks. | Re-resolve python at runtime, OR have `hive init` write the detected path into config and use it explicitly |

## Conclusion

Phase 1.2's **infrastructure goals** are met: memory lives in the workspace, init bootstraps mempalace automatically, the wing is a fixed `hive`, and the workspace's `mcp.json` correctly points at the local data dir. These were the user's stated headline requirements and they work.

Phase 1.2's **happy-path goal** (manager actually completes a tick + writes drawers + spawns worker + worker opens PR) was blocked by the manager-hang issue, which masked the worker-cwd and re-pend fixes (we couldn't verify them this run because no worker was spawned).

**Recommended next phase: Phase 1.3** — three focused fixes:
1. Manager skill tightening + watchdog timeout adjustment to make ticks reliably complete within budget
2. Unbuffered manager output so `manager.log` survives SIGKILL (essential for diagnosing future hangs)
3. `hive status` reads workspace-local memory (small Go fix)

After Phase 1.3, re-run this runbook to verify the full happy path actually works.
