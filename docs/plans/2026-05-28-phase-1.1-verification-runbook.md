# Phase 1.1 Verification Runbook — Executed

**Date executed:** 2026-05-28
**Hive binary commit:** `1d8092b` (`fix: disambiguate v2 skill names from legacy capstone-hive`)
**Tested against:** claude `claude-opus-4-7`, mempalace gateway local (`MEMPALACE_ROOT=/Users/jannik/sft-capstone-memory`)
**Target repo:** local-only `/tmp/hive-verify-target` (single-commit init, non-bare)
**Predecessor:** [Phase 1 runbook](2026-05-27-phase-1-verification-runbook.md)

## Result summary

**Overall: PARTIAL PASS — significant progress, two new structural findings.**

**Phase 1 fixes from this spec that worked:**
1. ✅ Manager now actually invokes our v2 skill (after disambiguation fix)
2. ✅ Manager spawned a worker subprocess this time
3. ✅ Manager filed mempalace drawers (requirement, story, agent-state, diary entry) — all visible via the mempalace MCP
4. ✅ `hive init` auto-detected mempalace MCP from `~/.claude.json` and spliced the correct config into `.claude/mcp.json`
5. ✅ `hive init` warned about the non-bare local team repo
6. ✅ `hive stop` cleanly terminated everything (after a SIGKILL escalation on the watchdog — see issues)

**Fixed inline during this verification** (committed to main):
- `1d8092b` — Disambiguate skill names from `capstone-hive`. Phase 1.1 spec didn't anticipate that a globally-installed `sft-capstone-skills:capstone-hive` skill would be invoked instead of our workspace-local one because the description-matching heuristic favored the more-used global skill. Renamed our skills to `hive-v2-manager` / `hive-v2-junior` and updated the watchdog prompt to be explicit.

**New issues discovered (NOT fixed in this phase — file for Phase 1.2 or Phase 2):**

| # | Issue | Severity |
|---|---|---|
| 1 | Worker subprocess died immediately. Manager skill spawns worker with `cd repos/<team>--junior-<id>` then runs `claude --print` — but claude's sandbox in that cwd can't read workspace-root paths like `.hive/agents/<id>/context.md`. Worker exits before doing any work. Manager self-diagnosed this in its log. | **HIGH** |
| 2 | Story drawer remained `assigned` after worker died. Manager skill doesn't re-pend on worker exit without `exit_reason=completed` verification. Story is now stuck. | **HIGH** |
| 3 | `mempalace_add_drawer` writes to ChromaDB only, NOT to on-disk `.md` files. Phase 0 spec §9 assumed all drawers were file-based — but only `mempalace_remember` (findings) produces `.md` files. `hive status` reads from disk → won't see structured drawers from skill writes. | **HIGH (architectural)** |
| 4 | `hive stop` required SIGKILL on the watchdog (graceful SIGTERM didn't take in 30s). Probably a signal-handler quirk in the watchdog's select loop. | MEDIUM |
| 5 | Tick duration 4m42s — longer than P1 (3m31s). Manager doing more correct work (drawers + worker spawn) takes more LLM time. Above the 5-min hard cap in some configurations. | LOW |

## Phase 1.1 deltas vs Phase 1

| Check | Phase 1 actual | Phase 1.1 result |
|---|---|---|
| `hive init` auto-detected mempalace | n/a | ✅ printed `mempalace MCP: auto-configured from ~/.claude.json` |
| `hive init` warned about non-bare repo | n/a | ✅ printed clear warning to stderr |
| Manager invoked workspace v2 skill (not legacy capstone-hive) | n/a | ✅ after disambiguation fix `1d8092b` |
| Manager spawned worker subprocess | ❌ no | ✅ spawned `agent-426bec5d` |
| Mempalace wing created | ❌ no | ✅ `hive-verify` wing visible via MCP (4 drawers) |
| Requirement drawer | ❌ no | ✅ `drawer_hive-verify_requirements_f4ca8394...` |
| Story drawer | ❌ no | ✅ `drawer_hive-verify_stories_6bd174fc...` (status=assigned, stuck) |
| Agent-state drawer | ❌ no | ✅ `drawer_hive-verify_agents_dd581496...` (status=exited, exit_reason=completed but worker did NOT actually complete) |
| Diary entry | ❌ no | ✅ `manager tick-end spawned=1 reaped=1 live=0 pending=0` |
| Worker actually did the work | n/a | ❌ died before doing anything (CWD sandbox issue) |
| PR opened | n/a | ❌ blocked by worker death |

## What the manager actually did this tick

From `.hive/manager.log`:

```
Tick complete. Exiting.

**Summary**
- Drained 1 inbox file (`req-1779956506-a7ae2b49.txt` → `processed/`); filed `requirement` + `story` drawers in `hive-verify`.
- Spawned 1 junior worker `agent-426bec5d` in worktree `repos/test-team--junior-426bec5d` on branch `agent/test-team--junior-426bec5d`; filed `agent-state` drawer; flipped story to `assigned`.
- Reaped same worker within the tick — it died before doing any work because `claude --print` was launched with CWD inside the worktree, so its allowed-directory sandbox refused to read `/private/tmp/hive-verify-ws/.hive/agents/426bec5d/context.md`. Per Phase 1.1 the agent-state is marked `exited / completed`, worktree removed, agent dir cleaned.
- Diary: `manager  tick-end  spawned=1 reaped=1 live=0 pending=0`.

**Heads-up for the skill author (not fixed — would require Edit, which is forbidden in-tick):** the spawn recipe in `.claude/skills/manager.md` step 2 puts the worker's CWD inside the worktree, so its session sandbox can't read `.hive/agents/<id>/context.md` at the workspace root. Result: every junior dies on the first Read/Bash and the story is left `assigned` with no live agent (no logic re-pends it). Two viable fixes: (a) launch with `cwd=<workspace root>` and have the junior `cd repos/<team>--junior-<id>` itself, or (b) pass the context inline as the prompt instead of via a file reference. Worth filing as a finding next tick.
```

This is excellent self-diagnosis — the manager respected the no-Edit constraint, completed the tick anyway, and surfaced the exact root cause + two viable fixes for the next pass.

## Pre-flight (executed)

- [x] `which claude` → `/opt/homebrew/bin/claude`
- [x] `MEMPALACE_ROOT=/Users/jannik/sft-capstone-memory` (set explicitly)
- [x] Test repo: `/tmp/hive-verify-target` (local, non-bare — triggers the warning by design)

## Conclusion + recommended next phase

Phase 1.1's **named goals** were met:
- The skill rewrite made the manager actually follow the orchestration pattern (visible drawer writes + worker spawn).
- The init auto-detect makes the workspace mcp.json work out of the box.
- The non-bare warning fires as designed.

But verification surfaced **three new HIGH-severity issues** that weren't in scope:
- **Worker spawn CWD bug** — manager skill needs to spawn workers with `cwd=workspace root` (skill rewrite, ~5 lines)
- **No re-pend on worker death** — manager skill needs to detect short-lived exits and revert story to `pending` with `retry_count++` (skill rewrite + drawer schema use)
- **`hive status` won't see structured drawers** — `mempalace_add_drawer` doesn't write on-disk .md files. The Phase 0 spec's "no MCP client in the CLI" decision needs to be revisited.

**Recommended next phase: Phase 1.2 (one more focused fix pass) BEFORE Phase 2.**

Scope:
- Manager skill: fix worker spawn cwd (option (a) from the manager's self-diagnosis)
- Manager skill: detect short-lived worker exits → re-pend story → bump retry_count → escalate after 3
- CLI: decide on `hive status` data source — either talk to mempalace MCP (introduces a Go MCP client dependency) or use `mempalace_remember` for everything (changes drawer semantics; structured drawers become "tagged findings")
- Re-verification gate: a worker actually opens a PR (real-remote test repo this time)

If Phase 1.2 fixes those three, Phase 2 (full role set, tech-lead decomposition, QA flow) becomes meaningful work on a working foundation. Without them, Phase 2 just adds more failure surface.

## Time to first action

- Watchdog start → manager tick exit: **4m42s**
- The manager's longer runtime is the result of actually doing the prescribed work (vs Phase 1 where it shortcut to just editing the file).
- Worker spawn → worker death: **<1s** (immediate sandbox rejection)
